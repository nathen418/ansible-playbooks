---
- hosts: all
  vars:
    ansible_host_key_checking: false
  roles:
    - including_vars

  become: true
  tasks:
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
